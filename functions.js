//функции в JS:

let a = 5
let b = 3

// (a,b) - при объявлении функции называются параметрами
function sum(a,b) {
    const c = a + b
    console.log(c)
}
sum(a, b)
8

//при вызове функции 8 и 12 называются аргументами
sum(8, 12)
20